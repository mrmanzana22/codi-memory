# Environment variables (NUNCA subir)
.env
.env.local
.env.*

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Databases and artifacts
*.db
*.db-wal
*.db-shm
*.db-journal
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.sqlite3-journal
data/

# Runtime config (local only)
.codi-memory.pid
.write_mode
.remember_mode
.access_tracking_mode
.destructive_guard
.toolset
.sharpe_mode
qdrant_data/

# Backups (G-02: timestamped backups contain PII)
memories_backup.json
memories_backup_*.json
server.py.backup*
server.py.bak.*
server.py.lazy_init_patch
*.bak
*.dump
*.sql.gz

# Runtime JSON data (local state, not code)
preguntas_curiosidad.json
pending_memories.json
mantenimiento.json
triggers.json
libros.json
recordatorios_pendientes.json
sharpe_baseline.json
m95_knowledge_import.json

# Session trackers (hooks)
hooks/.trackers/

# Generated reports (not source)
*_report.txt
*_baseline.txt

# Logs
*.log
logs/

# OS artifacts
.DS_Store
**/tmp/

# Session traces / telemetry
*.jsonl

# Claude Code server (subproyecto separado)
claude-code-server/

# macOS launch agents (local)
*.plist
backups/

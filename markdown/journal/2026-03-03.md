# Journal 2026-03-03


---
### 12:29 - benchmark_pgvector_final
**Que paso:** Benchmark pgvector FINAL con HNSW activado: Vector Search P50=2.30ms, FTS P50=0.79ms, Hybrid RRF P50=4.01ms. Fix critico: random_page_cost=1.1 (era 4.0 default). Con 5000 rows el HNSW Index Scan activo. Mejora 65x en vector search vs Seq Scan. Container codi-pgvector en Easypanel APROBADO para migracion. También se creo overload rrf_score(bigint).
**Por que importa:** GO/NO-GO gate de Fase 0 de la migracion pgvector PASADO. El stack nuevo es 22-65x mas rapido que sin HNSW. Confirma que PostgreSQL+pgvector reemplaza exitosamente SQLite+Qdrant+mem0.


---
### 12:43 - sprint_1_1_1_2_complete
**Que paso:** Sprint 1.1+1.2 de migracion pgvector COMPLETO. Creados 2 archivos nuevos: modules/config_pg.py (ConnectionPool psycopg3, singleton, auto pgvector register, random_page_cost=1.1, env CODI_PG_URL) y modules/pg_store.py (PGMemoryStore con 12 metodos: add, search hybrid RRF, delete, get_by_ids, scroll, query_vector, upsert, update_payload, count, search_fts, record_access, get_all). Singleton global 'pg'. Tests de integracion: health, add+search+fts+scroll+count+update+delete, semantic lifecycle, upsert cycle — ALL PASS. Dependencias instaladas en venv: psycopg[binary] 3.3.3, pgvector 0.4.2, psycopg_pool 3.3.0. No se toco ningun archivo existente.
**Por que importa:** Foundation layer de la migracion lista. pg_store.py es el drop-in replacement para mem0+Qdrant+FTS5. Todo el codigo futuro de migracion importa de aqui.


---
### 16:23 - tarea_completada
**Que paso:** Sprint 1.3 MIGRATION COMPLETE: Migrated ALL data from Qdrant+SQLite to PostgreSQL. 4400 episodic + 440 semantic from Qdrant, 104K rows from SQLite. 19/20 validation OK (tool_calls -9 rows from concurrent daemon writes during migration). Total time ~2h45m. DNS hiccup at 70K recovered automatically via psycopg_pool exponential backoff.
**Por que importa:** Fase 1 de pgvector migration COMPLETE. Toda la data ahora existe en PostgreSQL. Next step: Fase 2 - migrar los modulos core para usar pg_store en vez de Qdrant+SQLite+mem0.


---
### 17:09 - tarea_completada
**Que paso:** FASE 2 COMPLETE: 6 core modules migrated from Qdrant+mem0+SQLite to pg_store (PostgreSQL+pgvector). Sprints: 2.1 config.py flag, 2.2 semantic_store.py (4 funcs), 2.3 memory_core.py (21 funcs, fixed off-by-one in pg_store.search column indices), 2.4 memory_smart.py (search_fts→PG tsvector, add_memory_smart→pg.add, search_with_fts_content→pg.query_vector cosine), 2.5 consolidation.py (11 qdrant calls + 8 record_access → pg.*, _log_consolidation_run→PG), 2.6 write_worker.py verified (delegates to migrated modules). All 8 smoke tests pass.
**Por que importa:** This is the largest phase of the pgvector migration. Zero qdrant/mem0 references remain in the 6 core modules. The system now reads/writes exclusively through pg_store for all core memory operations. db_pool.py kept for auxiliary SQLite tables only.


---
### 17:29 - tarea_completada
**Que paso:** Fase 3 de migracion pgvector completada. 26 archivos migrados, ~140 refs legacy eliminadas. 10 agentes Sonnet en paralelo (2 batches de 5). Zero legacy refs en codigo ejecutable.
**Por que importa:** Toda la capa de modulos ahora usa pg_store unificado. Ya solo faltan: smoke test completo, parity test, restart del daemon, y cleanup final de Qdrant.


---
### 17:44 - tarea_completada
**Que paso:** pgvector migration Fases 0-4 COMPLETAS. MCP server reconectado y verificado live: 4401 memorias en PostgreSQL, search hybrid+ACT-R+semantic funcional. Qdrant Cloud ya no se usa en codigo ejecutable. 26 modulos migrados, ~140 refs legacy eliminadas, 30/30 imports pass, 5/5 live tests pass.
**Por que importa:** Hito mayor del proyecto codi-memory. Eliminamos dependencia de Qdrant Cloud + mem0 + SQLite FTS5. Stack unificado: PostgreSQL 17 + pgvector + psycopg3. Reduce costos, complejidad, y puntos de falla.

